import { AddDirectiveDirective } from './add-directive.directive';

describe('AddDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AddDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
